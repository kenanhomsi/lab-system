"use client";

import { PropsWithChildren } from "react";

/**
 * Reserved utility layer for availabilities table derived helpers.
 */
const Utils = ({ children }: PropsWithChildren) => <>{children}</>;

export { Utils };
