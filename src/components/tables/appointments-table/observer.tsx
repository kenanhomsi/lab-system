"use client";

import { PropsWithChildren } from "react";

/**
 * Reserved observer layer for appointments table cross-feature events.
 */
const Observer = ({ children }: PropsWithChildren) => <>{children}</>;

export default Observer;
