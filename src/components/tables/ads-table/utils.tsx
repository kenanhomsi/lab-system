"use client";

import { PropsWithChildren } from "react";

/**
 * Reserved utility layer for ads table derived helpers.
 */
const Utils = ({ children }: PropsWithChildren) => <>{children}</>;

export { Utils };
